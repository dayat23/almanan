<%= form_for(@photo_product) do |f| %>
  <% if @photo_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_product.errors.count, "error") %> prohibited this photo_product from being saved:</h2>

      <ul>
      <% @photo_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_product %><br />
    <%= f.text_field :image_product %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
